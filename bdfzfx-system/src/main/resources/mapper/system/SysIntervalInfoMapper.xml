<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bdfzfx.system.mapper.SysIntervalInfoMapper">
    
    <resultMap type="SysIntervalInfo" id="SysIntervalInfoResult">
        <result property="id"    column="id"    />
        <result property="stationId"    column="station_id"    />
        <result property="intervalId"    column="interval_id"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="selectSysIntervalInfoVo">
        select id, station_id, interval_id, create_by, create_time, update_by, update_time, remark from sys_interval_info
    </sql>

    <select id="selectSysIntervalInfoList" parameterType="SysIntervalInfo" resultMap="SysIntervalInfoResult">
        <include refid="selectSysIntervalInfoVo"/>
        <where>  
            <if test="stationId != null  and stationId != ''"> and station_id like concat('%', #{stationId}, '%')</if>
            <if test="intervalId != null  and intervalId != ''"> and interval_id like concat('%', #{intervalId}, '%')</if>
        </where>
    </select>
    
    <select id="selectSysIntervalInfoById" parameterType="Long" resultMap="SysIntervalInfoResult">
        <include refid="selectSysIntervalInfoVo"/>
        where id = #{id}
    </select>

    <insert id="insertSysIntervalInfo" parameterType="SysIntervalInfo" useGeneratedKeys="true" keyProperty="id">
        insert into sys_interval_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="stationId != null and stationId != ''">station_id,</if>
            <if test="intervalId != null and intervalId != ''">interval_id,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="stationId != null and stationId != ''">#{stationId},</if>
            <if test="intervalId != null and intervalId != ''">#{intervalId},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateSysIntervalInfo" parameterType="SysIntervalInfo">
        update sys_interval_info
        <trim prefix="SET" suffixOverrides=",">
            <if test="stationId != null and stationId != ''">station_id = #{stationId},</if>
            <if test="intervalId != null and intervalId != ''">interval_id = #{intervalId},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteSysIntervalInfoById" parameterType="Long">
        delete from sys_interval_info where id = #{id}
    </delete>

    <delete id="deleteSysIntervalInfoByIds" parameterType="String">
        delete from sys_interval_info where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>